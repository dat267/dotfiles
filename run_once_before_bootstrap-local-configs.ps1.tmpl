{{ if eq .chezmoi.os "windows" -}}
function Copy-IfMissing($src, $dst) {
    if ((Test-Path $src) -and -not (Test-Path $dst)) { Copy-Item $src $dst }
}

Copy-IfMissing "$HOME\.env.local.example"       "$HOME\.env.local"
Copy-IfMissing "$HOME\.gitconfig.local.example" "$HOME\.gitconfig.local"

$proxyEnabled = (Get-ItemProperty -Path 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Internet Settings' -ErrorAction SilentlyContinue).ProxyEnable
if ($proxyEnabled -eq 1) {
    Write-Host "⚠ Proxy detected — fill in ~/.env.local and ~/.gitconfig.local with your proxy credentials." -ForegroundColor Yellow
}
{{ end -}}

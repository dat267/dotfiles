#!/usr/bin/env bash
# Global Crush configuration — managed by chezmoi.

# ── Global context ─────────────────────────────────────────────────────
# Injected into every prompt to remind the model of available skills.
option global-context-path "$HOME/.config/crush/context.md"

# ── Skills ─────────────────────────────────────────────────────────────
# Local skills directory (relative to project root)
option skill-path .config/crush/skills
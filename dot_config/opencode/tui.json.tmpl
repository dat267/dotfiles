{
  "$schema": "https://opencode.ai/tui.json",
  "theme": {{ if eq .chezmoi.os "android" }}"system"{{ else }}"minimal"{{ end }},
  "diff_style": "stacked",
  "cursor": { "style": "default" },
  "mouse": false,
  "plugin": []
}